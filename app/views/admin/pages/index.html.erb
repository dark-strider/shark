<h1>Listing pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %></td>
    <td><%= link_to 'Show', [:admin, page] %></td>
    <td><%= link_to 'Edit', [:edit, :admin, page] %></td>
    <td><%= link_to 'Destroy', [:admin, page], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New page', [:new, :admin, :page] %>
