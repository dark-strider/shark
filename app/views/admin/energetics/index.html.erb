<h1>Listing energetics</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @energetics.each do |energetic| %>
  <tr>
    <td><%= energetic.name %></td>
    <td><%= link_to 'Show', [:admin, energetic] %></td>
    <td><%= link_to 'Edit', [:edit, :admin, energetic] %></td>
    <td><%= link_to 'Destroy', [:admin, energetic], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New energetic', [:new, :admin, :energetic] %>
